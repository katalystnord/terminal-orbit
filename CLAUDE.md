# terminal-orbit

A Rust TUI conversion of ORBIT, a 1999 OpenGL space combat simulator by Steve Belczyk.

## What this is

The original C source lives at `/home/david/code/space-orbit/` (~15,348 lines, 34 files). This project replaces all OpenGL rendering with a terminal UI: a 3D braille-rendered viewport via the `drawille` crate, and HUD panels via `ratatui`. Physics, AI, mission logic, and vector math are ported directly from C.

**Why:** Preserve and modernize a real-physics educational game (Newtonian flight, real solar system, campaigns from Mars to Pluto) without requiring a 1999 OpenGL binary.

## Stack

- **Rust** — OpenGL removal leaves nothing to salvage in the C rendering path
- **ratatui 0.29 + crossterm 0.28** — TUI windowing and HUD panels
- **drawille** (latest) — Unicode braille sub-pixel 3D viewport
- **serde / bincode** — save files and network packets
- **tokio** — Phase 10 multiplayer only
- Target: 30fps fixed-timestep game loop

## Key source files (original C)

| File | Lines | Role |
|---|---|---|
| `orbit.h` | ~755 | Master header — all structs, constants, array sizes. Primary reference for `types.rs` |
| `util.c` | ~255 | Vector math kernel — `RotateAbout` (Rodrigues), `Dotp`, `Crossp`, `Normalize`, etc. |
| `player.c` | ~300 | Newtonian and arcade flight physics, orientation via `RotateAbout` |
| `think.c` | ~405 | 8 AI strategies: DoNothing, Sit1–4, Hunt1–4; TurnToward, MoveToward, FindEnemy |
| `mission.c` | ~800 | Mission tokenizer + all keyword handlers; cursor relative/absolute system |
| `event.c` | ~200 | DoEvents trigger/action table |
| `hud.c` | ~750 | HUD rendering; `RadarCoords()` at lines 211–258 — port verbatim |
| `planet.c` | ~700 | Planet orbital mechanics and real solar system data |
| `orbit.c` | ~700 | Main entry point, DrawScene game loop, gravity |
| `ac3d.c` | ~700 | AC3D model loader |
| `model.c` | — | `.tri` model loader (9 floats + hex color per line, coords ÷100) |
| `stars.h` | — | Static C array of 2000 stars as [x, y, z, magnitude] unit vectors |

## Key constants (from `orbit.h`)

```
NPLANETS=32, NTARGETS=32, NMSLS=32, NBOOMS=32, NWAYPOINTS=32, NSTARS=2000
NWEAPONS=10, NCLIENTS=16
KM_TO_UNITS1=6000.0, KM_TO_UNITS2=6000.0/1e6
THETA=1.6          (rad/s rotation rate)
DELTAV=0.2         (km/s/s acceleration)
G=0.025, RMIN=2.0
BOOM_TIME=1.0s, CONSAGE=3.0s (console message fade), MAXDELTAT=0.1s
```

## Mission file format (`.msn`)

- Whitespace-tokenized, `/* */` comments, `Include filename` recursion
- Cursor system: bare value = absolute; `+value` = relative offset; planet name = planet position
- After `Cursor { Earth }`, position IS Earth's position; `{ Earth -15000 }` = Earth + offset
- `\\` in text = newline
- 52 mission files at `/home/david/code/space-orbit/missions/`

## 3D model formats

- `.tri`: rows of `x1 y1 z1  x2 y2 z2  x3 y3 z3  0xRRGGBB` — divide coords by 100
- `.ac`: AC3D format — MATERIAL/OBJECT/numvert/numsurf/SURF blocks, polygon soup
- Models at `/home/david/code/space-orbit/models/`
- Planet texture maps (PPM) at `/home/david/code/space-orbit/maps/` — **not needed**, wireframe spheres replace textured rendering
- When Phase 3/4 arrives, **copy** `missions/` and `models/` into this repo (do not symlink)

## Port vs. rewrite

| Direct port from C | Rewrite fresh |
|---|---|
| `util.c` vector math | DrawScene → crossterm game loop |
| `player.c` Newtonian/arcade physics | All OpenGL rendering |
| `think.c` AI strategies | HUD layout → ratatui |
| `mission.c` tokenizer logic | Planet rendering → wireframe circles |
| `event.c` trigger/action table | `ac3d.c` → edge-only wireframe loader |
| `hud.c` RadarCoords() math | Network → tokio async |
| Solar system constants from `planet.c` | `keyboard.c` → crossterm |

**Skip entirely:** `lights.c`, `sound.c` (terminal bell only), `joystick.c`, `screenshot.c`

## Planned module tree

```
terminal-orbit/
  Cargo.toml
  build.rs                    (star data codegen from stars.h)
  missions/                   (copied from space-orbit)
  models/                     (copied from space-orbit)
  src/
    main.rs
    app.rs
    constants.rs
    types.rs
    math.rs
    input.rs
    physics.rs
    ai.rs
    planet_data.rs
    star_data.rs              (generated by build.rs)
    combat/
      mod.rs, weapons.rs, missile.rs, explosions.rs, scoring.rs
    mission/
      mod.rs, parser.rs, loader.rs, events.rs, waypoints.rs
    model/
      mod.rs, tri_loader.rs, ac3d_loader.rs, bounding.rs
    renderer/
      mod.rs, projection.rs, canvas.rs, lines.rs, viewport.rs
      planets.rs, stars.rs, orbits.rs
    hud/
      mod.rs, radar.rs, panels.rs
    ui/
      mod.rs, title_screen.rs, briefing.rs, console.rs, orrery.rs
    net/
      mod.rs, protocol.rs, server.rs, client.rs
    save.rs
    prefs.rs
```

## Implementation phases

### Phase 0 — Math Kernel ✓ DONE
Port `util.c` to `src/math.rs`: `Vec3`, `RotateAbout` (Rodrigues formula), `Dotp`, `Crossp`, `Normalize`, `Mag`, `Perp`, `Vadd/Vsub/Vmul/Vdiv`. Create `Cargo.toml` with ratatui 0.29, crossterm 0.28, drawille (latest).

### Phase 1 — Core State & Physics (headless) ✓ DONE
- `src/types.rs`: all structs from `orbit.h` — `Player`, `Target`, `Missile`, `Planet`, `Weapon`, `Boom`, `Event`, `GameState`
- `src/constants.rs`: all numeric constants above
- `src/physics.rs`: `move_player()`, `gravity()`, `move_targets()`, `move_planets()`
- `src/ai.rs`: 8 strategies from `think.c`, `TurnToward`, `MoveToward`, `FindEnemy`
- `src/planet_data.rs`: real solar system — Sol, Mercury, Venus, Earth/Moon, Mars/Phobos/Deimos, Jupiter+4 moons, Saturn/Titan, Uranus, Neptune, Pluto

### Phase 2 — Terminal Window, Game Loop, Braille Viewport ✓ DONE
- `src/app.rs`: crossterm raw mode, alternate screen, 33ms fixed-timestep loop
- `src/input.rs`: keyboard state bitfield (WASD/IJKL fly, SPC fire, Z dense-stars, Q quit)
- `src/renderer/projection.rs`: camera transform, perspective divide, near-plane clip
- `src/renderer/canvas.rs`: drawille adapter — `set(x,y)` + `line(x1,y1,x2,y2)` + `rows()`
- `src/renderer/viewport.rs`: `draw_scene()`, `draw_edge_world()` (pub, used by planets)
- `src/hud/panels.rs`: ratatui layout — braille viewport + HUD strip

### Phase 3 — Model Loading & Wireframe Ships ✓ DONE
- `src/model/tri_loader.rs`: parse `.tri` format, deduplicate edges
- `src/model/ac3d_loader.rs`: parse AC3D, edge-only
- `src/renderer/viewport.rs`: `draw_target_ship()` using LookAt 3×3 rotation

### Phase 4 — Mission System & Event Engine ✓ DONE
- `src/mission/parser.rs`: tokenizer with Include stack, `/* */` stripping
- `src/mission/loader.rs`: all keyword handlers — cursor, player, waypoint, object, briefing, event, planet, weapon, include
- `src/mission/events.rs`: DoEvents — Alarm/Approach/Destroy/Score/Shields/StopNear triggers; Message/Hide/Unhide/LoadMission/Boom/MoveObject/Stop actions
- Cursor relative vs absolute correctly handled; train01–04 all load

### Phase 5 — Combat, Weapons, Explosions ✓ DONE
- `src/combat/weapons.rs`: `fire_missile()`
- `src/combat/missile.rs`: `move_missiles()` — planet/target/player hits, bounding sphere
- `src/combat/scoring.rs`: `destroy_target()`, score increment, Destroy event trigger
- `src/combat/explosions.rs`: `spawn_boom()`, `move_booms()`, `draw_booms()`, `draw_missiles()`
- Spacebar fires; player death → mission restart

### Phase 6 — Planets & Solar System ✓ DONE
- `src/renderer/planets.rs`: 5 LOD levels (12×12 → 8×8 → 4×4 → dot → skip)
- Oblicity tilt, ring systems (Jupiter/Saturn/Uranus/Neptune), orbital path circles
- `draw_edge_world` exposed as pub from viewport.rs

### Phase 7 — Stars, Radar, Full HUD ✓ DONE
- `build.rs`: parses `stars.h` → writes `star_data.rs` to OUT_DIR; Fibonacci fallback if stars.h absent
- `src/star_data.rs`: `include!(OUT_DIR/star_data.rs)`
- `src/renderer/stars.rs`: `all_stars()` + `draw_stars(dense)` — 200/2000 toggle (Z key)
- `src/hud/radar.rs`: exact port of `RadarCoords()` from `hud.c:211–258`; 21×11 char grid; blips: `·` planets, `+`/`f` enemies/friendlies, `X` locked, `o` missiles, `W` waypoint
- `src/hud/panels.rs`: radar panel left + stats right; `█`/`░` bars for shields/throttle; speed, score, weapon, locked target name+dist, waypoint compass arrow

**31 tests passing.**

### Phase 8 — Full Campaign
- `src/save.rs`: serde/TOML save slots 0–9
- `src/ui/title_screen.rs`: ASCII art title, menu (New/Load/Quit), rotating ship
- `src/ui/briefing.rs`: full-screen briefing before each mission (`\\` = newline)
- `src/ui/console.rs`: port `console.c` — circular buffer 10 messages, 3.0s fade
- Win/lose conditions: `win.msn`, `lose.msn`

**Done when:** play Mars→Pluto campaign, save mid-campaign, reload, complete.

### Phase 9 — Polish & Educational Mode
- `src/prefs.rs`: `~/.terminal-orbit.toml` — player name, flight model, gravity, key bindings
- `src/ui/orrery.rs`: top-down 2D solar system view (press O)
- Planet/target name overlays (press N)
- Space-junk motion cue (8 dots around viewport center)

**Done when:** orrery works, prefs persist, full educational feature set accessible.

### Phase 10 — Multiplayer
- `src/net/protocol.rs`: PositionUpdate, FireMissile, TargetDestroyed, MissionLoad, Ping/Pong, PlayerJoin/Leave
- `src/net/server.rs`: tokio async, accept connections, broadcast state
- `src/net/client.rs`: tokio async, periodic position reports, apply remote updates

**Done when:** two instances on LAN see each other's ships and can score kills.
